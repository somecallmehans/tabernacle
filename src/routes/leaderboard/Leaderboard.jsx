import React from "react";

export default function Leaderboard() {
  return (
    <h1>
      Leaderboard page which will be filterable by month. Backend is setup to do
      this but there isn't enough data to support it
    </h1>
  );
}
